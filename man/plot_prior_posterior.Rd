% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abc_stochastic_model.R
\name{plot_prior_posterior}
\alias{plot_prior_posterior}
\title{Plot Prior and Posterior Distributions with Optional True Value}
\usage{
plot_prior_posterior(prior_values, posterior_values, true_value = NULL)
}
\arguments{
\item{prior_values}{A numeric vector of samples from the prior distribution.}

\item{posterior_values}{A numeric vector of samples from the posterior distribution.}

\item{true_value}{Optional numeric value representing the true parameter value to be shown as a vertical dashed line.}
}
\value{
A \code{ggplot2} object representing the density plot comparing the prior and posterior distributions.
}
\description{
This function generates a density plot comparing the prior and posterior distributions
of a parameter. An optional vertical dashed line can be added to indicate the true parameter value.
}
\details{
The function uses \code{geom_density()} to visualize both prior and posterior samples, and \code{geom_vline()} to optionally display the true parameter value. It uses \code{theme_minimal()} and places the legend at the bottom for clarity.
}
\examples{
prior <- rnorm(1000, mean = 0, sd = 2)
posterior <- rnorm(1000, mean = 1, sd = 0.5)
true_value <- 1.2
plot_prior_posterior(prior, posterior, true_value)

}
